# UI 设计规范与重构计划

**版本**: V3.2  
**创建日期**: 2026-01-06  
**最后更新**: 2026-01-13  
**状态**: ✅ Phase 6 已完成（Vue 3 前端重构完成）

> **V3.2 更新**：
> - Vue 3 前端重构完成，完全复刻原版 HTML 样式
> - 创建完整的 Pinia 状态管理（user、focus、pet）
> - 所有页面已迁移：Login、StudyRoom、StudyCabin、Focus、Pet、Stats、Profile、Member、Shredder、QRCodeScan
> - API 模块完善：auth、focus、pet、stats、qrcode
> - ECharts 图表集成（学习统计页面）
>
> **V3.1 更新**：
> - 宠物显示优化：使用可爱动物 GIF 动图替代 emoji
> - 新增"返回小屋"自定义模态框，显示时间和经验转化提示
> - 经验计算规则：1分钟以上才有经验，秒数向上取整（如1分23秒→2分钟经验）
> - 碎纸机音效功能（/audio/shredder.mp3）
> - 修复 /app/ 路由 404 问题
>
> **V3.0 更新**：技术栈升级为 React 18 + TypeScript + Vite 5 + Tailwind CSS。前端项目独立于 `cloud-study-room-ui/` 目录，开发阶段使用 5173 端口，生产环境构建后部署到 `static/app/` 目录，访问 `http://localhost:9090/app/`。

---

## 一、设计目标

### 1.1 核心理念

将现有的"功能堆砌式"界面重构为**苹果风格的极简设计系统**，实现：

1. **移动端优先**：底部导航架构，单手操作友好
2. **视觉层级清晰**：大留白、圆角卡片、清晰的信息层级
3. **情感化设计**：温暖的色调、流畅的动效、舒适的交互反馈
4. **无障碍友好**：更大字号、更好对比度、触控区域充足

### 1.2 设计关键词

- **简洁** (Clean)
- **温暖** (Warm)
- **专注** (Focused)
- **呼吸感** (Breathing)

---

## 二、设计系统 (Design System)

### 2.1 色彩体系

#### 主色调

| 用途 | 色值 | 说明 |
|------|------|------|
| 主色 (Primary) | `#7C3AED` | 紫罗兰色，用于强调、按钮、进度条 |
| 主色浅 | `#A78BFA` | 用于hover状态、次要强调 |
| 主色深 | `#5B21B6` | 用于pressed状态 |

#### 中性色

| 用途 | 色值 | 说明 |
|------|------|------|
| 背景色 | `#FFFFFF` | 纯白背景 |
| 卡片背景 | `#F9FAFB` | 浅灰卡片 |
| 分割线 | `#E5E7EB` | 细微分割 |
| 次要文字 | `#9CA3AF` | 辅助说明文字 |
| 主要文字 | `#1F2937` | 标题、正文 |
| 深色文字 | `#111827` | 重要标题 |

#### 功能色

| 用途 | 色值 | 说明 |
|------|------|------|
| 成功 | `#10B981` | 完成、正确 |
| 警告 | `#F59E0B` | 提醒、注意 |
| 错误 | `#EF4444` | 错误、危险 |
| 信息 | `#3B82F6` | 提示、链接 |

### 2.2 字体规范

#### 字体家族

```css
font-family: -apple-system, BlinkMacSystemFont, 'SF Pro Display', 
             'PingFang SC', 'Helvetica Neue', sans-serif;
```

#### 字号层级

| 级别 | 字号 | 字重 | 行高 | 用途 |
|------|------|------|------|------|
| Display | 32px | 700 | 1.2 | 页面主标题 |
| H1 | 24px | 600 | 1.3 | 卡片标题 |
| H2 | 20px | 600 | 1.4 | 区块标题 |
| Body Large | 17px | 400 | 1.5 | 重要正文 |
| Body | 15px | 400 | 1.5 | 普通正文 |
| Caption | 13px | 400 | 1.4 | 辅助说明 |
| Small | 11px | 400 | 1.3 | 标签、时间戳 |

### 2.3 间距系统

基于 4px 网格系统：

| Token | 值 | 用途 |
|-------|-----|------|
| `--space-1` | 4px | 紧凑间距 |
| `--space-2` | 8px | 元素内间距 |
| `--space-3` | 12px | 小间距 |
| `--space-4` | 16px | 标准间距 |
| `--space-5` | 20px | 中等间距 |
| `--space-6` | 24px | 大间距 |
| `--space-8` | 32px | 区块间距 |
| `--space-10` | 40px | 页面边距 |

### 2.4 圆角规范

| Token | 值 | 用途 |
|-------|-----|------|
| `--radius-sm` | 8px | 小按钮、标签 |
| `--radius-md` | 12px | 输入框、小卡片 |
| `--radius-lg` | 16px | 卡片、弹窗 |
| `--radius-xl` | 24px | 大卡片、底部弹窗 |
| `--radius-full` | 9999px | 圆形按钮、头像 |

### 2.5 阴影规范

```css
/* 卡片阴影 - 轻微 */
--shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);

/* 卡片阴影 - 标准 */
--shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 
             0 2px 4px -1px rgba(0, 0, 0, 0.06);

/* 卡片阴影 - 悬浮 */
--shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1), 
             0 4px 6px -2px rgba(0, 0, 0, 0.05);

/* 底部导航阴影 */
--shadow-nav: 0 -4px 20px rgba(0, 0, 0, 0.08);
```

### 2.6 动效规范

```css
/* 标准过渡 */
--transition-fast: 150ms ease;
--transition-normal: 250ms ease;
--transition-slow: 350ms ease;

/* 弹性动效 */
--transition-bounce: 500ms cubic-bezier(0.34, 1.56, 0.64, 1);
```

---

## 三、导航架构重构

### 3.1 底部导航栏设计

采用 iOS 风格的底部 Tab Bar，固定在屏幕底部。

```
┌─────────────────────────────────────────────────────────┐
│                      页面内容区                          │
│                                                         │
│                                                         │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  ┌─────┐   ┌─────┐   ┌─────┐   ┌─────┐                 │
│  │ 🏠  │   │ 📚  │   │ 👑  │   │ 👤  │                 │
│  │首页 │   │小屋 │   │会员 │   │我的 │                 │
│  └─────┘   └─────┘   └─────┘   └─────┘                 │
└─────────────────────────────────────────────────────────┘
```

#### 导航项配置

| Tab | 图标 | 标签 | 对应页面 | 功能说明 |
|-----|------|------|----------|----------|
| 1 | 🏠 | 自习室 | `index.html` | 扫码入座 + 座位状态 |
| 2 | 📚 | 学习小屋 | `study.html` | 专注工具集合 |
| 3 | 👑 | 会员 | `member.html` | 会员权益（预留） |
| 4 | 👤 | 我的 | `profile.html` | 个人中心 |

### 3.2 各模块功能划分

#### Tab 1: 自习室首页 (`index.html`)

**核心功能**：
- 动态二维码展示与扫码入座
- 座位状态总览
- 快速入座引导

**页面结构**：
```
┌─────────────────────────────────────┐
│  云端自习室                    [通知] │  <- 顶部导航
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │      欢迎回来，[用户名]       │   │  <- 问候卡片
│  │      今日已学习 2小时30分     │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │         [二维码区域]          │   │  <- 扫码入座卡片
│  │                              │   │
│  │      点击扫码入座            │   │
│  │      当前空闲座位: 5/8       │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  座位状态                           │
│  ┌───┐ ┌───┐ ┌───┐ ┌───┐         │  <- 座位网格
│  │A01│ │A02│ │B01│ │B02│         │
│  └───┘ └───┘ └───┘ └───┘         │
└─────────────────────────────────────┘
```

#### Tab 2: 学习小屋 (`study.html`)

**核心功能**：
- 专注模式入口（跳转到沉浸式专注页面）
- 白噪音电台（独立页面）
- 情绪碎纸机
- 电子宠物详情
- 学习统计

**页面结构**：
```
┌─────────────────────────────────────┐
│  学习小屋                           │  <- 顶部标题
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  🎯 开始专注                  │   │  <- 主按钮（大）
│  │     进入沉浸式专注空间         │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  学习工具                           │
│  ┌─────────┐  ┌─────────┐         │
│  │ 🎵      │  │ 📝      │         │  <- 工具网格
│  │白噪音   │  │碎纸机   │         │
│  └─────────┘  └─────────┘         │
│  ┌─────────┐  ┌─────────┐         │
│  │ 🐱      │  │ 📊      │         │
│  │宠物详情 │  │学习统计 │         │
│  └─────────┘  └─────────┘         │
└─────────────────────────────────────┘
```

---

## 三、沉浸式专注模式设计（核心重构）

### 3.1 设计理念

专注模式不再是简单的计时器页面，而是一个**沉浸式专注空间**，整合：
- 正向计时器（核心）
- 白噪音混音控制
- **实时互动宠物**（始终陪伴）
- 专注数据实时反馈

### 3.2 页面结构

```
┌─────────────────────────────────────────────────────────┐
│  ← 返回                              今日: 2h30m  ⭐128 │  <- 顶部状态栏
├─────────────────────────────────────────────────────────┤
│                                                         │
│                      ┌─────────┐                       │
│                      │  45:32  │                       │  <- 大计时器
│                      │ 专注中...│                       │
│                      └─────────┘                       │
│                                                         │
│                   [暂停]  [停止]                        │  <- 控制按钮
│                                                         │
│        ┌─────┐                                         │
│        │ 🐱 │  ← 可拖动的实时宠物                      │  <- 浮动宠物
│        │    │    (自主行为 + 互动)                     │
│        └─────┘                                         │
│                                                         │
├─────────────────────────────────────────────────────────┤
│  🎵 白噪音                                              │  <- 底部控制面板
│  ┌────┐ ┌────┐ ┌────┐ ┌────┐    🔊 ━━━━○━━━         │
│  │🌧️ │ │🔥 │ │🌲 │ │🌊 │                           │
│  └────┘ └────┘ └────┘ └────┘                           │
└─────────────────────────────────────────────────────────┘
```

### 3.3 实时宠物系统

#### 核心特性

| 特性 | 说明 |
|------|------|
| **可拖动** | 用户可以拖动宠物到屏幕任意位置 |
| **自主行为** | 宠物会自己走动、玩耍、打盹、发呆 |
| **状态感知** | 根据专注状态改变行为（专注时安静陪伴，暂停时活跃） |
| **互动反馈** | 点击/拖动时有反应，弹出气泡对话 |
| **实时同步** | 经验值、等级、心情与后端实时同步 |

#### 宠物行为状态机

```
                    ┌──────────────┐
                    │     idle     │ <- 默认状态：原地待机
                    └──────┬───────┘
                           │
         ┌─────────────────┼─────────────────┐
         │                 │                 │
         ▼                 ▼                 ▼
   ┌──────────┐     ┌──────────┐     ┌──────────┐
   │ walking  │     │ playing  │     │ sleeping │
   │  走动    │     │  玩耍    │     │  打盹    │
   └──────────┘     └──────────┘     └──────────┘
         │                 │                 │
         └─────────────────┴─────────────────┘
                           │
                           ▼
                    ┌──────────────┐
                    │  interacting │ <- 用户互动时
                    └──────────────┘
```

#### 宠物动画帧（CSS Sprite / Lottie）

| 状态 | 动画 | 时长 | 触发条件 |
|------|------|------|----------|
| idle | 呼吸/眨眼 | 2s loop | 默认 |
| walking | 左右走动 | 3s | 随机触发 |
| playing | 跳跃/打滚 | 2s | 心情高时 |
| sleeping | 打盹 | 5s | 长时间专注 |
| happy | 开心跳跃 | 1s | 获得经验时 |
| interact | 挥手/点头 | 0.5s | 用户点击 |

#### 宠物气泡对话

```javascript
const PET_DIALOGUES = {
    focus_start: [
        "加油！我陪着你！",
        "一起努力吧~",
        "今天也要好好学习哦！"
    ],
    focus_milestone: {  // 专注里程碑
        15: ["15分钟了！继续保持~", "你真棒！"],
        30: ["半小时了！休息一下？", "太厉害了！"],
        60: ["一小时！你是学霸！", "佩服佩服~"]
    },
    idle: [
        "...", "zzZ", "~♪", "(*^▽^*)"
    ],
    interact: [
        "嘿嘿~", "摸摸头~", "喵~", "汪！"
    ]
};
```

### 3.4 技术实现方案

#### 方案选择：Vue.js CDN + CSS 动画

**理由**：
1. 宠物状态管理复杂，Vue 的响应式更适合
2. 无需 Node.js 构建，保持轻量
3. 组件化便于维护
4. 与现有 Spring Boot 架构兼容

#### 核心组件结构

```
focus.html (Vue App)
├── FocusTimer.vue      # 计时器组件
├── FloatingPet.vue     # 浮动宠物组件
│   ├── PetSprite       # 宠物精灵动画
│   ├── PetBubble       # 气泡对话
│   └── DragHandler     # 拖动处理
├── SoundDock.vue       # 白噪音控制
└── StatsBar.vue        # 顶部状态栏
```

#### 宠物拖动实现

```javascript
// 拖动逻辑（支持触摸和鼠标）
const useDraggable = (elementRef) => {
    const position = reactive({ x: 100, y: 300 });
    const isDragging = ref(false);
    
    const onStart = (e) => {
        isDragging.value = true;
        // 触发互动动画
        emit('interact');
    };
    
    const onMove = (e) => {
        if (!isDragging.value) return;
        const { clientX, clientY } = e.touches ? e.touches[0] : e;
        position.x = clientX - 40;  // 居中偏移
        position.y = clientY - 40;
        // 边界检测
        position.x = Math.max(0, Math.min(window.innerWidth - 80, position.x));
        position.y = Math.max(60, Math.min(window.innerHeight - 160, position.y));
    };
    
    const onEnd = () => {
        isDragging.value = false;
    };
    
    return { position, isDragging, onStart, onMove, onEnd };
};
```

#### 宠物自主行为 AI

```javascript
// 简单行为 AI
class PetBehaviorAI {
    constructor(pet) {
        this.pet = pet;
        this.state = 'idle';
        this.stateTimer = null;
    }
    
    // 根据环境决定下一个行为
    decideNextAction(context) {
        const { isFocusing, focusDuration, mood } = context;
        
        if (isFocusing) {
            // 专注时：安静陪伴，偶尔打盹
            if (focusDuration > 30 * 60 && Math.random() < 0.3) {
                return 'sleeping';
            }
            return Math.random() < 0.8 ? 'idle' : 'walking';
        } else {
            // 非专注时：更活跃
            const actions = ['idle', 'walking', 'playing'];
            const weights = mood > 70 ? [0.3, 0.3, 0.4] : [0.5, 0.3, 0.2];
            return this.weightedRandom(actions, weights);
        }
    }
    
    // 定时切换状态
    startBehaviorLoop() {
        const tick = () => {
            const nextAction = this.decideNextAction(this.getContext());
            this.setState(nextAction);
            
            // 随机间隔 3-8 秒
            const delay = 3000 + Math.random() * 5000;
            this.stateTimer = setTimeout(tick, delay);
        };
        tick();
    }
}
```

#### Tab 3: 会员中心 (`member.html`)

**核心功能**（预留）：
- 会员权益展示
- 套餐购买
- 优惠券

**页面结构**：
```
┌─────────────────────────────────────┐
│  会员中心                           │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  👑 开通会员                  │   │  <- 会员卡片
│  │     解锁更多专属功能           │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  会员权益                           │
│  • 无限专注时长                     │
│  • 专属白噪音                       │
│  • 宠物皮肤                         │
│  • 数据云同步                       │
├─────────────────────────────────────┤
│  套餐选择                           │
│  ┌─────┐ ┌─────┐ ┌─────┐         │
│  │月卡 │ │季卡 │ │年卡 │         │
│  │¥29 │ │¥79 │ │¥199│         │
│  └─────┘ └─────┘ └─────┘         │
└─────────────────────────────────────┘
```

#### Tab 4: 个人中心 (`profile.html`)

**核心功能**：
- 用户信息展示
- 学习成就
- 设置
- 留言板/反馈

**页面结构**：
```
┌─────────────────────────────────────┐
│  个人中心                           │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  [头像]                       │   │  <- 用户卡片
│  │  用户名                       │   │
│  │  累计学习 128小时 | Lv.15    │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  ┌─────────┐  ┌─────────┐         │
│  │ 7天     │  │ 15级    │         │  <- 成就展示
│  │连续学习 │  │当前等级 │         │
│  └─────────┘  └─────────┘         │
├─────────────────────────────────────┤
│  功能列表                           │
│  ┌─────────────────────────────┐   │
│  │ 📋 我的订单              >  │   │
│  ├─────────────────────────────┤   │
│  │ 💬 留言板                >  │   │
│  ├─────────────────────────────┤   │
│  │ ⚙️ 设置                  >  │   │
│  ├─────────────────────────────┤   │
│  │ ❓ 帮助与反馈            >  │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

---

## 四、宠物详情页设计 (`pet.html`)

### 4.1 功能定位

宠物详情页是宠物的"个人主页"，用于：
- 查看宠物完整信息（等级、经验、进化阶段）
- 宠物互动（喂食、抚摸、对话）
- 查看成长日志
- 宠物装扮（预留）

### 4.2 页面结构

```
┌─────────────────────────────────────┐
│  ← 返回                    ⚙️ 设置  │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │                              │   │
│  │        [宠物大图]            │   │  <- 宠物展示区
│  │         🐱                   │   │     (带动画)
│  │      "喵~ 今天也要加油哦"    │   │
│  │                              │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐   │
│  │  小橘子           Lv.15     │   │  <- 信息卡片
│  │  ━━━━━━━━━━━○━━━ 75/100 EXP │   │
│  │  成年体 · 心情😊 95%         │   │
│  └─────────────────────────────┘   │
├─────────────────────────────────────┤
│  互动                               │
│  ┌─────┐ ┌─────┐ ┌─────┐ ┌─────┐ │
│  │ 🍖 │ │ 👋 │ │ 💬 │ │ 🎮 │ │  <- 互动按钮
│  │喂食 │ │抚摸 │ │聊天 │ │玩耍 │ │
│  └─────┘ └─────┘ └─────┘ └─────┘ │
├─────────────────────────────────────┤
│  成长日志                           │
│  ┌─────────────────────────────┐   │
│  │ 🎉 升级到 Lv.15      今天    │   │
│  │ ⭐ 获得 50 经验值     2小时前 │   │
│  │ 🐣 进化为成年体       昨天    │   │
│  └─────────────────────────────┘   │
└─────────────────────────────────────┘
```

### 4.3 互动功能

| 互动 | 效果 | 冷却 |
|------|------|------|
| 喂食 | 心情+20，播放吃东西动画 | 30分钟 |
| 抚摸 | 心情+10，播放开心动画 | 5分钟 |
| 聊天 | 随机对话，心情+5 | 无 |
| 玩耍 | 心情+15，播放玩耍动画 | 15分钟 |

---

## 五、组件规范

### 4.1 底部导航栏组件

```html
<!-- 底部导航栏 -->
<nav class="bottom-nav">
    <a href="index.html" class="nav-item active">
        <span class="nav-icon">🏠</span>
        <span class="nav-label">自习室</span>
    </a>
    <a href="study.html" class="nav-item">
        <span class="nav-icon">📚</span>
        <span class="nav-label">学习小屋</span>
    </a>
    <a href="member.html" class="nav-item">
        <span class="nav-icon">👑</span>
        <span class="nav-label">会员</span>
    </a>
    <a href="profile.html" class="nav-item">
        <span class="nav-icon">👤</span>
        <span class="nav-label">我的</span>
    </a>
</nav>
```

```css
.bottom-nav {
    position: fixed;
    bottom: 0;
    left: 0;
    right: 0;
    height: 83px; /* 包含安全区域 */
    padding-bottom: env(safe-area-inset-bottom);
    background: rgba(255, 255, 255, 0.95);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    display: flex;
    justify-content: space-around;
    align-items: center;
    box-shadow: var(--shadow-nav);
    z-index: 1000;
}

.nav-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 4px;
    padding: 8px 16px;
    text-decoration: none;
    color: var(--color-text-secondary);
    transition: var(--transition-fast);
}

.nav-item.active {
    color: var(--color-primary);
}

.nav-icon {
    font-size: 24px;
}

.nav-label {
    font-size: 11px;
    font-weight: 500;
}
```

### 4.2 卡片组件

```css
.card {
    background: var(--color-card-bg);
    border-radius: var(--radius-lg);
    padding: var(--space-5);
    box-shadow: var(--shadow-sm);
}

.card-elevated {
    background: #FFFFFF;
    box-shadow: var(--shadow-md);
}

.card-interactive {
    cursor: pointer;
    transition: var(--transition-normal);
}

.card-interactive:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg);
}

.card-interactive:active {
    transform: translateY(0);
}
```

### 4.3 按钮组件

```css
/* 主要按钮 */
.btn-primary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 8px;
    padding: 16px 32px;
    background: var(--color-primary);
    color: white;
    border: none;
    border-radius: var(--radius-md);
    font-size: 17px;
    font-weight: 600;
    cursor: pointer;
    transition: var(--transition-fast);
}

.btn-primary:hover {
    background: var(--color-primary-light);
}

.btn-primary:active {
    background: var(--color-primary-dark);
    transform: scale(0.98);
}

/* 次要按钮 */
.btn-secondary {
    background: var(--color-card-bg);
    color: var(--color-primary);
    border: 1px solid var(--color-border);
}

/* 大按钮 */
.btn-large {
    width: 100%;
    padding: 20px;
    font-size: 18px;
    border-radius: var(--radius-lg);
}
```

### 4.4 工具网格组件

```css
.tool-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--space-4);
}

.tool-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: var(--space-3);
    padding: var(--space-6);
    background: var(--color-card-bg);
    border-radius: var(--radius-lg);
    text-decoration: none;
    color: var(--color-text-primary);
    transition: var(--transition-normal);
}

.tool-card:hover {
    background: #FFFFFF;
    box-shadow: var(--shadow-md);
}

.tool-icon {
    font-size: 32px;
}

.tool-label {
    font-size: 15px;
    font-weight: 500;
}
```

---

## 六、技术架构升级

### 6.1 技术栈选择

| 层级 | 技术 | 说明 |
|------|------|------|
| **前端框架** | **React 18 + TypeScript** | 组件化开发，类型安全 |
| **构建工具** | **Vite 5** | 快速开发服务器，热更新 |
| **样式框架** | **Tailwind CSS 3.4** | 原子化 CSS，快速开发 |
| **图标库** | **Lucide React** | 现代化图标 |
| **图表库** | **Recharts** | React 图表组件 |
| **动画** | CSS Animation + Framer Motion | 流畅动效 |
| **后端** | Spring Boot | 保持不变 |
| **数据库** | MySQL + MyBatis-Plus | 保持不变 |

### 6.2 项目结构

```
cloud-study-room-ui/
├── src/
│   ├── components/
│   │   ├── layout/
│   │   │   ├── BottomNav.tsx      # 底部导航
│   │   │   └── PageContainer.tsx  # 页面容器
│   │   ├── study-room/            # 自习室组件
│   │   ├── study-cabin/           # 学习小屋组件
│   │   ├── member/                # 会员中心组件
│   │   └── profile/               # 个人中心组件
│   ├── pages/
│   │   ├── StudyRoom.tsx          # 自习室首页
│   │   ├── StudyCabin.tsx         # 学习小屋
│   │   ├── Member.tsx             # 会员中心
│   │   └── Profile.tsx            # 个人中心
│   ├── contexts/
│   │   └── AppContext.tsx         # 全局状态管理
│   ├── services/
│   │   └── api.ts                 # API 调用封装
│   ├── types/
│   │   └── index.ts               # TypeScript 类型定义
│   ├── lib/
│   │   └── utils.ts               # 工具函数
│   ├── App.tsx                    # 应用入口
│   └── main.tsx                   # 渲染入口
├── vite.config.ts                 # Vite 配置（含 API 代理）
├── tailwind.config.js             # Tailwind 配置
├── tsconfig.json                  # TypeScript 配置
└── package.json                   # 依赖管理
```

### 6.3 开发与部署流程

**开发阶段**：
```bash
cd cloud-study-room-ui
npm run dev
# 前端运行在 http://localhost:5173
# API 请求自动代理到 http://localhost:9090
```

**生产部署**：
```bash
cd cloud-study-room-ui
npm run build
# 将 dist/ 目录内容复制到 src/main/resources/static/
# 用户只需访问 http://localhost:9090
```

### 6.4 API 代理配置

```typescript
// vite.config.ts
export default defineConfig({
  server: {
    proxy: {
      '/api': {
        target: 'http://localhost:9090',
        changeOrigin: true,
      },
      '/upload': {
        target: 'http://localhost:9090',
        changeOrigin: true,
      },
    },
  },
});
```

---

## 七、页面重构计划

### 7.1 重构优先级

| 优先级 | 页面 | 说明 | 预计工时 | 技术 |
|--------|------|------|----------|------|
| P0 | 公共组件 | CSS变量、基础样式、底部导航 | 2h | CSS |
| P0 | `focus.html` | **沉浸式专注（计时器+白噪音+宠物）** | 6h | Vue 3 |
| P0 | `index.html` | 自习室首页（合并原qrcode.html） | 3h | 原生 JS |
| P0 | `study.html` | 学习小屋（新建，工具入口） | 2h | 原生 JS |
| P1 | `pet.html` | 宠物详情页优化 | 3h | Vue 3 |
| P1 | `profile.html` | 个人中心（新建） | 2h | 原生 JS |
| P2 | `stats.html` | 统计页面优化 | 2h | Vue 3 |
| P2 | `member.html` | 会员中心（预留框架） | 1h | 原生 JS |
| P2 | `shredder.html` | 碎纸机页面优化 | 1h | 原生 JS |

### 7.2 文件结构调整

**调整前**：
```
static/
├── login.html
├── qrcode.html      # 扫码大厅
├── focus.html       # 专注模式（简单计时器）
├── pet.html         # 电子宠物（静态展示）
├── shredder.html    # 情绪碎纸机
├── stats.html       # 学习统计
└── css/
    └── ...
```

**调整后**：
```
static/
├── login.html           # 登录页（保留）
├── index.html           # 自习室首页（Tab 1）
├── study.html           # 学习小屋（Tab 2，新建）
├── member.html          # 会员中心（Tab 3，新建）
├── profile.html         # 个人中心（Tab 4，新建）
├── focus.html           # 沉浸式专注（Vue 3，重构）
├── pet.html             # 宠物详情（Vue 3，重构）
├── shredder.html        # 情绪碎纸机（优化）
├── stats.html           # 学习统计（Vue 3，优化）
├── css/
│   ├── variables.css    # CSS变量（新建）
│   ├── components.css   # 公共组件（新建）
│   ├── pet-sprites.css  # 宠物动画（新建）
│   └── ...
├── js/
│   ├── nav.js           # 导航逻辑（新建）
│   ├── pet-ai.js        # 宠物行为AI（新建）
│   └── ...
└── assets/
    └── pets/            # 宠物资源（新建）
        ├── cat/
        ├── dog/
        └── rabbit/
```

### 7.3 页面跳转流程

```
                    ┌─────────────┐
                    │  login.html │
                    └──────┬──────┘
                           │ 登录成功
                           ▼
┌──────────────────────────────────────────────────────────┐
│                    底部导航栏                              │
├──────────┬──────────┬──────────┬──────────┬─────────────┤
│          │          │          │          │             │
│  index   │  study   │  member  │ profile  │             │
│  首页    │  小屋    │  会员    │  我的    │             │
│          │          │          │          │             │
│  • 二维码│  • 专注  │  • 套餐  │  • 信息  │             │
│  • 座位  │  • 白噪音│  • 权益  │  • 设置  │             │
│          │  • 碎纸机│          │  • 留言  │             │
│          │  • 宠物  │          │          │             │
│          │  • 统计  │          │          │             │
└──────────┴────┬─────┴──────────┴──────────┴─────────────┘
                │
                │ 点击"开始专注"
                ▼
        ┌───────────────────────────────┐
        │        focus.html             │
        │   沉浸式专注空间 (Vue 3)       │
        │                               │
        │  ┌─────────────────────────┐ │
        │  │      计时器 45:32       │ │
        │  │                         │ │
        │  │   🐱 ← 可拖动宠物       │ │
        │  │      (自主行为)         │ │
        │  │                         │ │
        │  │  🎵 白噪音控制面板      │ │
        │  └─────────────────────────┘ │
        └───────────────────────────────┘
                │
                │ 点击宠物详情
                ▼
        ┌───────────────────────────────┐
        │        pet.html               │
        │   宠物详情页 (Vue 3)          │
        │   - 完整信息                  │
        │   - 喂食/抚摸/玩耍            │
        │   - 成长日志                  │
        └───────────────────────────────┘
```

---

## 八、执行步骤

### Phase 1: 基础设施（Day 1）

1. **创建 CSS 变量文件** `css/variables.css`
   - 定义所有颜色、间距、圆角、阴影变量
   
2. **创建公共组件文件** `css/components.css`
   - 底部导航栏样式
   - 卡片组件样式
   - 按钮组件样式
   - 工具网格样式

3. **创建宠物动画样式** `css/pet-sprites.css`
   - 宠物各状态动画
   - 气泡对话样式
   - 拖动效果

4. **创建导航 JS** `js/nav.js`
   - 导航高亮逻辑
   - 页面切换动效

### Phase 2: 核心功能重构（Day 2-4）

1. **重构 `focus.html`**（重点，Vue 3）
   - 计时器组件
   - 浮动宠物组件（可拖动+自主行为）
   - 白噪音控制组件
   - 顶部状态栏

2. **重构 `index.html`**（原 qrcode.html）
   - 引入新设计系统
   - 添加底部导航
   - 重新布局二维码区域

3. **新建 `study.html`**
   - 专注入口大按钮
   - 工具网格

### Phase 3: 辅助页面（Day 5-6）

1. **重构 `pet.html`**（Vue 3）
   - 宠物详情展示
   - 互动功能（喂食、抚摸、玩耍）
   - 成长日志

2. **新建 `profile.html`**
   - 用户信息卡片
   - 功能列表

3. **优化其他页面**
   - stats.html（Vue 3 + ECharts）
   - shredder.html
   - member.html（预留框架）

### Phase 4: 测试与优化（Day 7）

1. **功能测试**
   - 所有页面跳转正常
   - 宠物拖动、自主行为正常
   - 数据交互正常

2. **性能优化**
   - 动画流畅度
   - 加载速度

3. **视觉优化**
   - 动效调整
   - 细节打磨

---

## 九、宠物资源规划

### 9.1 宠物类型

| 类型 | 基础形态 | 进化形态 | 职业形态 |
|------|----------|----------|----------|
| 猫 | 🥚→🐱→😺→😸 | 学霸猫🎓 | 可扩展 |
| 狗 | 🥚→🐶→🐕→🦮 | 学霸狗🎓 | 可扩展 |
| 兔 | 🥚→🐰→🐇→🐇 | 学霸兔🎓 | 可扩展 |

### 9.2 动画资源（CSS 实现）

由于使用 Emoji 作为宠物形象，动画通过 CSS transform 实现：

```css
/* 宠物动画状态 */
.pet-sprite {
    font-size: 60px;
    transition: transform 0.3s;
}

/* 待机 - 呼吸效果 */
.pet-sprite.idle {
    animation: pet-breathe 2s ease-in-out infinite;
}
@keyframes pet-breathe {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.05); }
}

/* 走动 */
.pet-sprite.walking {
    animation: pet-walk 0.5s ease-in-out infinite;
}
@keyframes pet-walk {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-5px); }
}

/* 玩耍 - 跳跃 */
.pet-sprite.playing {
    animation: pet-jump 0.8s ease-in-out infinite;
}
@keyframes pet-jump {
    0%, 100% { transform: translateY(0) rotate(0deg); }
    50% { transform: translateY(-20px) rotate(10deg); }
}

/* 睡觉 */
.pet-sprite.sleeping {
    animation: pet-sleep 3s ease-in-out infinite;
    opacity: 0.8;
}
@keyframes pet-sleep {
    0%, 100% { transform: rotate(-5deg); }
    50% { transform: rotate(5deg); }
}

/* 互动 - 开心 */
.pet-sprite.interact {
    animation: pet-happy 0.5s ease;
}
@keyframes pet-happy {
    0% { transform: scale(1); }
    50% { transform: scale(1.2); }
    100% { transform: scale(1); }
}

/* 朝向 */
.floating-pet.left .pet-sprite {
    transform: scaleX(-1);
}
```

### 9.3 气泡对话样式

```css
.pet-bubble {
    position: absolute;
    bottom: 100%;
    left: 50%;
    transform: translateX(-50%);
    background: white;
    padding: 8px 14px;
    border-radius: 16px;
    font-size: 13px;
    color: #333;
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
    white-space: nowrap;
    opacity: 0;
    transition: opacity 0.3s, transform 0.3s;
    pointer-events: none;
}

.pet-bubble.visible {
    opacity: 1;
    transform: translateX(-50%) translateY(-5px);
}

.pet-bubble::after {
    content: '';
    position: absolute;
    top: 100%;
    left: 50%;
    transform: translateX(-50%);
    border: 6px solid transparent;
    border-top-color: white;
}
```

---

## 十、注意事项

### 10.1 兼容性要求

- 支持 iOS Safari 13+
- 支持 Android Chrome 80+
- 支持微信内置浏览器

### 10.2 性能要求

- 首屏加载 < 2s
- 页面切换 < 300ms
- 动效流畅无卡顿

### 10.3 安全区域适配

```css
/* iOS 安全区域适配 */
padding-bottom: env(safe-area-inset-bottom);
padding-top: env(safe-area-inset-top);
```

### 10.4 保留原有功能

- 所有后端 API 接口保持不变
- 业务逻辑（锁定、专注、宠物）保持不变
- 仅重构前端 UI 层

---

## 十一、验收标准

1. **视觉一致性**：所有页面遵循统一设计系统
2. **导航流畅性**：底部导航切换无闪烁、无延迟
3. **功能完整性**：原有功能全部正常运行
4. **移动端适配**：在主流手机上显示正常
5. **代码质量**：CSS 变量化、组件化、可维护
6. **宠物系统**：
   - 宠物可拖动到屏幕任意位置
   - 宠物有自主行为（走动、玩耍、打盹）
   - 点击宠物有互动反馈
   - 专注时宠物安静陪伴
7. **专注模式**：
   - 计时器正常运行
   - 白噪音可混合播放
   - 宠物实时显示
   - 数据正确保存

---

## 十二、后续扩展（预留）

### 12.1 宠物系统扩展

- 宠物皮肤/装扮系统
- 宠物技能（如：提醒休息、鼓励加油）
- 宠物社交（查看好友宠物）
- 宠物成就系统

### 12.2 专注模式扩展

- 番茄钟模式
- 专注音乐电台
- 专注挑战/排行榜
- 专注房间（多人一起专注）

---

*本文档为 UI 重构的指导规范，执行时可根据实际情况微调*
